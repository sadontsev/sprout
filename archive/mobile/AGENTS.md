> [!WARNING]
> **Archived — not maintained.** The live app is [`native/`](../../native/) (SwiftUI, iOS + macOS).
> These instructions describe the retired Expo app and are kept only so the decisions they explain
> stay readable. Do not follow them for new work; the root `CLAUDE.md` is the current guidance.

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.
