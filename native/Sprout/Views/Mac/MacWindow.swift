#if os(macOS)
import SwiftUI

// PHASE-A STUB — replaced by the real NavigationSplitView shell in the next commit.
struct MacWindow: View {
    let model: AppModel
    let explore: ExploreModel
    var body: some View { Text(verbatim: "MacWindow") }
}
#endif
