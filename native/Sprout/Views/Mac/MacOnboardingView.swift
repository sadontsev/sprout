#if os(macOS)
import SwiftUI

// PHASE-A STUB — replaced by the real onboarding window (1e).
struct MacOnboardingView: View {
    let model: AppModel
    var body: some View { Text(verbatim: "MacOnboarding") }
}
#endif
